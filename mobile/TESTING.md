# 📱 Быстрый старт: Как протестировать мобильное приложение

Это краткое руководство для быстрого запуска и тестирования мобильного приложения.

## ⚡ Самый быстрый способ (5 минут)

### Требования:
- Смартфон с WiFi
- Компьютер и телефон в одной WiFi сети
- Node.js установлен на компьютере

### Шаги:

#### 1. Установите Expo Go на телефон

📱 **iPhone**: [App Store - Expo Go](https://apps.apple.com/app/expo-go/id982107779)
📱 **Android**: [Google Play - Expo Go](https://play.google.com/store/apps/details?id=host.exp.exponent)

#### 2. Установите зависимости

```bash
cd mobile
npm install
```

#### 3. Настройте API URL

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Узнайте ваш IP адрес
# macOS/Linux:
ifconfig | grep "inet " | grep -v 127.0.0.1

# Windows:
ipconfig

# Отредактируйте .env файл и замените YOUR_LOCAL_IP на ваш IP:
# API_URL=http://192.168.1.100:30002/api
```

**⚠️ ВАЖНО:** Используйте IP адрес вашего компьютера, НЕ localhost!

#### 4. Запустите backend сервер

В отдельном терминале:

```bash
# Из корневой директории проекта
cd backend && npm run dev &
cd ../gateway && npm run dev &

# Проверьте что сервер работает:
curl http://localhost:30002/api/health
```

#### 5. Запустите мобильное приложение

```bash
cd mobile
npm start
```

#### 6. Отсканируйте QR-код

В терминале появится QR-код:

- **iPhone**: Откройте **Камеру** и наведите на QR-код
- **Android**: Откройте **Expo Go** → нажмите **Scan QR code**

🎉 **Готово!** Приложение загрузится на вашем телефоне.

---

## 🧪 Что тестировать

### 1. Регистрация нового пользователя

1. На экране входа нажмите **"Sign Up"**
2. Введите:
   - Email: `test@example.com`
   - Password: `123456`
3. Нажмите **"Sign Up"**
4. Вы должны автоматически войти в приложение

### 2. Навигация

После входа проверьте все табы внизу экрана:
- **Home** - Dashboard с обзором
- **Habits** - Список привычек
- **Tasks** - Список задач
- **Profile** - Ваш профиль

### 3. Pull-to-Refresh

На любом экране потяните вниз для обновления данных.

### 4. Выход из аккаунта

1. Перейдите на вкладку **Profile**
2. Прокрутите вниз
3. Нажмите **"Logout"**
4. Подтвердите выход

### 5. Повторный вход

1. Войдите с теми же данными:
   - Email: `test@example.com`
   - Password: `123456`
2. Вы должны войти в систему

---

## 🐛 Частые проблемы и решения

### "Unable to connect to server"

**Причина:** Приложение не может подключиться к backend.

**Решение:**
1. Убедитесь что backend запущен:
   ```bash
   curl http://localhost:30002/api/health
   ```
2. Проверьте что IP в `.env` правильный
3. Убедитесь что телефон и компьютер в одной WiFi сети
4. Попробуйте отключить firewall временно

### Приложение зависает на "Building JavaScript bundle"

**Решение:**
```bash
# Остановите сервер (Ctrl+C) и запустите с очисткой кэша:
npm start -- --reset-cache
```

### QR-код не сканируется

**Решение:**
1. Попробуйте ввести URL вручную в Expo Go
2. URL показан под QR-кодом, например: `exp://192.168.1.100:19000`

### "Network request failed" при логине

**Причина:** Неправильный API_URL в конфигурации.

**Решение:**
1. Остановите приложение (Ctrl+C)
2. Проверьте `.env` файл - должен быть ваш локальный IP
3. Перезапустите: `npm start -- --reset-cache`

---

## 📊 Проверка соединения

### Проверьте backend:

```bash
# Должен вернуть: {"status": "ok"}
curl http://localhost:30002/api/health
```

### Проверьте с другого устройства в сети:

```bash
# Замените YOUR_IP на IP вашего компьютера
curl http://YOUR_IP:30002/api/health
```

Если вторая команда не работает - проблема в firewall или сетевых настройках.

---

## 🔥 Hot Reload

После запуска приложения, вы можете редактировать код и изменения автоматически применятся:

1. Откройте файл `src/screens/home/HomeScreen.tsx`
2. Измените текст "Welcome back" на "Hello"
3. Сохраните файл
4. Приложение автоматически обновится на телефоне!

---

## 🎯 Альтернативные способы тестирования

Если у вас нет смартфона или возникли проблемы с Expo Go:

### Вариант 1: Android эмулятор (Windows/Mac/Linux)

Требуется Android Studio.

```bash
# После установки Android Studio и создания эмулятора:
npm run android
```

[Инструкция по установке Android Studio](README.md#способ-2-android-emulator-android-studio)

### Вариант 2: iOS симулятор (только Mac)

Требуется Xcode.

```bash
# После установки Xcode:
npm run ios
```

[Инструкция по установке Xcode](README.md#способ-3-ios-simulator-только-macos)

### Вариант 3: Веб-версия (экспериментально)

```bash
npm run web
```

Откроется в браузере на `http://localhost:19006`

⚠️ Некоторые функции могут не работать в веб-версии.

---

## 📸 Ожидаемый результат

После успешного запуска вы должны увидеть:

1. **Экран входа** с полями Email и Password
2. После входа - **Dashboard** с приветствием
3. **4 вкладки внизу**: Home, Habits, Tasks, Profile
4. **Pull-to-refresh** работает на всех экранах
5. В **Profile** можно выйти из аккаунта

---

## 💡 Советы для разработки

### Откройте Dev Menu на устройстве:

- **iOS**: Встряхните телефон
- **Android**: Встряхните телефон или нажмите аппаратную кнопку меню

В Dev Menu доступны:
- Reload - перезагрузить приложение
- Debug Remote JS - отладка в Chrome
- Toggle Element Inspector - инспектор элементов
- Toggle Performance Monitor - монитор производительности

### Просмотр логов:

```bash
# В терминале где запущен npm start, логи будут отображаться автоматически

# Или используйте:
npx react-native log-android  # для Android
npx react-native log-ios       # для iOS
```

### Отладка в Chrome:

1. Откройте Dev Menu на устройстве
2. Выберите "Debug Remote JS"
3. Откроется Chrome с DevTools
4. Используйте Console для просмотра логов и ошибок

---

## 🎓 Следующие шаги

После успешного тестирования базового функционала:

1. 📖 Изучите [полную документацию](README.md)
2. 🔧 Попробуйте изменить код и посмотреть hot reload в действии
3. 🎨 Настройте цвета и стили в компонентах
4. 🚀 Добавьте новые функции (например, создание привычек)
5. 📱 Соберите production версию для тестирования на реальных устройствах

---

## 📚 Дополнительные ресурсы

- [Expo Go Tutorial](https://docs.expo.dev/get-started/expo-go/)
- [React Native Tutorial](https://reactnative.dev/docs/tutorial)
- [Debugging Guide](https://reactnative.dev/docs/debugging)

---

## 🆘 Нужна помощь?

1. Проверьте [README.md](README.md) для детальной информации
2. Посмотрите раздел [Решение проблем](README.md#-решение-проблем)
3. Откройте issue в репозитории проекта

**Удачного тестирования! 🚀**
