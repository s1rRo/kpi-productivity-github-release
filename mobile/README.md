# KPI Productivity Mobile App

React Native мобильное приложение для системы KPI Productivity, построенное с использованием Expo.

## 📱 Обзор

Это нативное мобильное приложение для iOS и Android, которое подключается к KPI Productivity Backend API. Приложение позволяет пользователям отслеживать привычки, задачи и KPI в удобном мобильном интерфейсе.

## 🛠 Технологический стек

- **React Native** - Фреймворк для мобильной разработки
- **Expo** - Набор инструментов для упрощения разработки
- **TypeScript** - Типизированный JavaScript
- **React Navigation** - Навигация между экранами
- **Axios** - HTTP клиент для API запросов
- **AsyncStorage** - Локальное хранилище данных

## 📋 Требования

Перед началом убедитесь, что установлены:

- **Node.js** 18+ и npm
- **Expo CLI**: `npm install -g expo-cli`
- Один из следующих вариантов для тестирования:
  - **Expo Go app** на вашем мобильном устройстве ([iOS](https://apps.apple.com/app/expo-go/id982107779) / [Android](https://play.google.com/store/apps/details?id=host.exp.exponent))
  - **Android Studio** с Android эмулятором
  - **Xcode** с iOS симулятором (только macOS)

## 🚀 Установка

### 1. Установите зависимости

```bash
cd mobile
npm install
```

### 2. Настройте подключение к API

Скопируйте файл с примером конфигурации:

```bash
cp .env.example .env
```

**ВАЖНО:** Отредактируйте `.env` файл и укажите правильный IP-адрес:

```env
API_URL=http://YOUR_LOCAL_IP:30002/api
```

**Как найти ваш локальный IP:**

**macOS/Linux:**
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

**Windows:**
```bash
ipconfig
```

Найдите IPv4 адрес в разделе WiFi или Ethernet (например, `192.168.1.100`).

**⚠️ НЕ используйте `localhost` или `127.0.0.1`** - мобильное устройство не сможет подключиться к вашему компьютеру через localhost!

### 3. Убедитесь, что Backend запущен

Перед запуском мобильного приложения убедитесь, что backend и gateway работают:

```bash
# В корневой директории проекта
cd backend && npm run dev &
cd ../gateway && npm run dev &
```

Проверьте, что API доступен:
```bash
curl http://localhost:30002/api/health
```

## 📱 Способы тестирования мобильного приложения

### Способ 1: Expo Go (Рекомендуется для начинающих)

**Самый простой способ** - не требует установки эмуляторов!

#### Шаг 1: Установите Expo Go на ваше устройство

- **iOS**: [Загрузите из App Store](https://apps.apple.com/app/expo-go/id982107779)
- **Android**: [Загрузите из Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

#### Шаг 2: Подключитесь к той же WiFi сети

Ваш компьютер и мобильное устройство **должны быть в одной WiFi сети**.

#### Шаг 3: Запустите приложение

```bash
cd mobile
npm start
```

Или используйте:
```bash
npm run start
```

#### Шаг 4: Отсканируйте QR-код

После запуска в терминале появится QR-код:

- **iOS**: Откройте камеру и отсканируйте QR-код
- **Android**: Откройте Expo Go и используйте встроенный сканер

Приложение автоматически загрузится на вашем устройстве!

**Преимущества:**
- ✅ Быстрая установка
- ✅ Реальное тестирование на устройстве
- ✅ Горячая перезагрузка (hot reload)
- ✅ Нет необходимости в эмуляторах

**Ограничения:**
- ⚠️ Устройство и компьютер должны быть в одной сети
- ⚠️ Некоторые нативные модули могут работать некорректно

---

### Способ 2: Android Emulator (Android Studio)

Для полного контроля и тестирования специфичных функций Android.

#### Шаг 1: Установите Android Studio

1. Загрузите [Android Studio](https://developer.android.com/studio)
2. Во время установки включите:
   - Android SDK
   - Android SDK Platform
   - Android Virtual Device

#### Шаг 2: Настройте переменные окружения

**macOS/Linux** (~/.bash_profile или ~/.zshrc):
```bash
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
```

**Windows**:
- Добавьте в PATH: `%LOCALAPPDATA%\Android\Sdk\platform-tools`

Перезапустите терминал после изменений.

#### Шаг 3: Создайте виртуальное устройство (AVD)

1. Откройте Android Studio
2. Перейдите в **Tools → Device Manager**
3. Нажмите **Create Device**
4. Выберите устройство (например, Pixel 5)
5. Выберите system image (рекомендуется API 30 или выше)
6. Нажмите **Finish**

#### Шаг 4: Запустите эмулятор

```bash
# Через Android Studio: нажмите кнопку play возле устройства
# Или через командную строку:
emulator -avd YOUR_AVD_NAME
```

#### Шаг 5: Запустите приложение

```bash
cd mobile
npm run android
```

Приложение автоматически установится и запустится в эмуляторе.

**Преимущества:**
- ✅ Полная функциональность Android
- ✅ Не требуется физическое устройство
- ✅ Отладка через Chrome DevTools

**Требования:**
- 🔧 Требует 8+ GB RAM
- 🔧 Занимает ~10 GB дискового пространства
- 🔧 Может быть медленным на старых компьютерах

---

### Способ 3: iOS Simulator (только macOS)

Для тестирования на iOS без физического iPhone.

#### Шаг 1: Установите Xcode

1. Откройте **App Store** на Mac
2. Найдите и установите **Xcode** (бесплатно, ~15 GB)
3. После установки откройте Xcode и примите лицензию

#### Шаг 2: Установите Command Line Tools

```bash
xcode-select --install
```

#### Шаг 3: Запустите симулятор

```bash
# Через Xcode: Xcode → Open Developer Tool → Simulator
# Или через командную строку:
open -a Simulator
```

Выберите модель iPhone в меню симулятора (рекомендуется iPhone 14).

#### Шаг 4: Запустите приложение

```bash
cd mobile
npm run ios
```

Приложение автоматически установится в симулятор.

**Преимущества:**
- ✅ Полная функциональность iOS
- ✅ Быстрая работа на Mac
- ✅ Отладка через Safari Developer Tools

**Требования:**
- 🔧 Требуется macOS
- 🔧 Требуется ~15 GB для Xcode

---

### Способ 4: Expo Dev Build (Advanced)

Для тестирования кастомных нативных модулей.

```bash
# Создайте dev build
expo prebuild
expo run:android  # или expo run:ios

# Или соберите APK/IPA
eas build --platform android --profile development
```

Подробнее в [Expo документации](https://docs.expo.dev/develop/development-builds/introduction/).

---

## 🐛 Отладка

### Включить Dev Menu

- **iOS**: Нажмите `Cmd + D` в симуляторе или встряхните устройство
- **Android**: Нажмите `Cmd + M` (Mac) или `Ctrl + M` (Windows/Linux), или встряхните устройство

### Просмотр логов

```bash
# Expo логи
npm start

# React Native логи напрямую
npx react-native log-android  # для Android
npx react-native log-ios       # для iOS
```

### Инспектор элементов

В Dev Menu выберите **"Toggle Element Inspector"** для анализа компонентов.

### Отладка сети (Network Debugging)

В Dev Menu выберите **"Debug Remote JS"** и откройте Chrome DevTools на `localhost:19000/debugger-ui`.

---

## 🔧 Решение проблем

### Проблема: "Unable to connect to server"

**Решение:**
1. Проверьте, что backend запущен: `curl http://localhost:30002/api/health`
2. Убедитесь, что в `.env` указан правильный IP (не localhost!)
3. Проверьте firewall - он может блокировать соединения
4. Убедитесь, что устройство и компьютер в одной WiFi сети

### Проблема: Expo Go зависает на "Building JavaScript bundle"

**Решение:**
```bash
# Очистите кэш
expo start -c

# Или
npm start -- --reset-cache
```

### Проблема: Android эмулятор не запускается

**Решение:**
1. Проверьте, что виртуализация включена в BIOS
2. Попробуйте увеличить RAM для AVD (в Device Manager)
3. Используйте более новый API level (30+)

### Проблема: iOS симулятор показывает черный экран

**Решение:**
```bash
# Сбросьте симулятор
xcrun simctl shutdown all
xcrun simctl erase all
```

---

## 📚 Структура проекта

```
mobile/
├── App.tsx                    # Точка входа приложения
├── src/
│   ├── components/            # Переиспользуемые компоненты
│   ├── constants/             # Константы и конфигурация
│   │   └── config.ts         # API endpoints и настройки
│   ├── contexts/              # React контексты
│   │   └── AuthContext.tsx   # Контекст аутентификации
│   ├── navigation/            # Навигация
│   │   └── AppNavigator.tsx  # Основной навигатор
│   ├── screens/               # Экраны приложения
│   │   ├── auth/             # Экраны аутентификации
│   │   ├── home/             # Dashboard
│   │   ├── habits/           # Управление привычками
│   │   ├── tasks/            # Управление задачами
│   │   └── profile/          # Профиль пользователя
│   ├── services/              # API сервисы
│   │   └── api.ts            # API клиент
│   ├── types/                 # TypeScript типы
│   └── utils/                 # Утилиты
├── assets/                    # Изображения, шрифты
├── app.json                   # Конфигурация Expo
├── package.json               # Зависимости
└── tsconfig.json              # Конфигурация TypeScript
```

---

## 🎨 Основные фичи

- ✅ Аутентификация (Login/Register)
- ✅ Dashboard с обзором
- ✅ Управление привычками
- ✅ Управление задачами
- ✅ Профиль пользователя
- ✅ Безопасное хранение токенов
- ✅ Автоматическое обновление токена
- ✅ Pull-to-refresh на всех экранах
- ✅ Обработка ошибок

---

## 🔐 Безопасность

- Токены хранятся в AsyncStorage (зашифрованное хранилище)
- HTTPS для production API
- Автоматическая проверка токена при запуске
- Timeout для API запросов (10 секунд)

---

## 📝 Команды для разработки

```bash
# Запустить в режиме разработки
npm start

# Запустить на Android
npm run android

# Запустить на iOS
npm run ios

# Запустить в веб-браузере
npm run web

# Запустить тесты
npm test

# Линтинг кода
npm run lint

# Очистить кэш
npm start -- --reset-cache
```

---

## 🚀 Развертывание

### Сборка для production

```bash
# Установите EAS CLI
npm install -g eas-cli

# Войдите в Expo аккаунт
eas login

# Настройте проект
eas build:configure

# Соберите для Android
eas build --platform android

# Соберите для iOS
eas build --platform ios
```

Подробнее в [Expo EAS документации](https://docs.expo.dev/build/introduction/).

---

## 🤝 Содействие

1. Создайте feature branch: `git checkout -b feature/amazing-feature`
2. Коммитьте изменения: `git commit -m 'Add amazing feature'`
3. Запушьте в branch: `git push origin feature/amazing-feature`
4. Откройте Pull Request

---

## 📄 Лицензия

MIT License - см. [LICENSE](../LICENSE) файл в корне проекта.

---

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте [Expo документацию](https://docs.expo.dev/)
2. Посмотрите [React Native документацию](https://reactnative.dev/docs/getting-started)
3. Откройте issue в репозитории проекта

---

## 📖 Полезные ссылки

- [Expo Documentation](https://docs.expo.dev/)
- [React Native Documentation](https://reactnative.dev/)
- [React Navigation](https://reactnavigation.org/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Expo Go App - iOS](https://apps.apple.com/app/expo-go/id982107779)
- [Expo Go App - Android](https://play.google.com/store/apps/details?id=host.exp.exponent)
